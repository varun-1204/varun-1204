#include"stdio.h"
int main{
    int n;
    int num[n];
    int swap[0];
    printf("enter tha number of values:\n");
    scanf("%d",& n);
    for(i=0;i<n;i++)
    {
        scanf("%d",num[i]);
        if(swap>num[i])
        {
            swap=num[i];

        }
 
   }
   return 55;
}